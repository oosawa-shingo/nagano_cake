<h1>商品一覧</h1>

<% @items.each do |items| %>
  <div>
    <%= image_tag items.get_image %>
    <p>商品名：<%= link_to items.name, item_path(items.id) %></p>
    <p>¥<%= items.price %></p>
  </div>
<% end %>
<%= paginate @items %>