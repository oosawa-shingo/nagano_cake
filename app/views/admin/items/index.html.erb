<h1>商品一覧</h1>

<h3><%= link_to "新規登録", new_admin_item_path %></h3>
<% @items.each do |items| %>
  <div>
    <p>商品ID：<%= items.id %></p>
    <p>商品名：<%= link_to items.name, admin_item_path(items.id) %></p>
    <p>税抜価格：<%= items.price %></p>
  </div>
<% end %>
<%= paginate @items %>