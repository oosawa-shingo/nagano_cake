<h1>商品詳細</h1>
<div>
  <%= image_tag @item.get_image %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <p>税抜価格(税込価格)：<%= @item.price.to_s(:dalimited) %>(<%= @item.add_tax_price.to_s(:dalimited) %>)円</p>
</div>
<%= link_to "編集する", edit_admin_item_path, :class => "btn btn-success" %>