<h1>注文履歴詳細</h1>

<div>
  <h4>注文情報</h4>
    <p>注文日：<%= @order.created_at.strftime("%Y/%m/%d") %></p>
    <p>配送先：
      <%= @order.postal_code %>
      <%= @order.address %>
      <%= @order.name %>
    </p>
    <p>支払方法：<%= @order.payment_method_i18n %></p>
</div>

<div>
  <h4>注文内容</h4>
    <% @order_detail.each do |order_detail| %>
    <div>
      <p>商品：<%= order_detail.item.name %></p>
      <p>単価（税込）：<%= order_detail.price.to_s(:dalimited) %></p>
      <p>個数：<%= order_detail.amount %></p>
      <p>小計：<%= order_detail.item.with_tax_price * order_detail.amount %></p>
    </div>
    <% end %>
</div>

<div>
  <h4>請求情報</h4>
    <p>商品合計：<%= @total %>円</p>

    <p>送料： 800 円</p>

    <p>請求金額合計：<%= @total + 800 %></p>
</div>

